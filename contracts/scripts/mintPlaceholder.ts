import type { NetworkProvider } from '@ton/blueprint';

/**
 * Jetton mint flows will be implemented with BURN Jetton Master (see P5-1-1-2).
 */
export async function run(_provider: NetworkProvider) {
    console.log('[mint] Not implemented yet — add after BURN Jetton Master + Wallet.');
}
