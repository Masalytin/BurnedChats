import { NANO_PER_BURN } from '../helpers';

/** Default large-tx threshold used in specs (10 BURN). */
export const LARGE_TX_THRESHOLD_10_BURN = 10n * NANO_PER_BURN;

/** Default activity threshold for “high activity” burn bonus in specs. */
export const ACTIVITY_THRESHOLD_DEFAULT = 100n;
